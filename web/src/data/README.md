# data/

`board.json` is synced verbatim from `design/board.json` (the ground-truth
board geometry — see CLAUDE.md). If the design copy changes, re-copy it
here; this directory is what actually ships with the frontend build.
Same pattern as `functions/data/`.
